
class Text extends Node {
  auto dragHandle = <div style=style.textHandle></div>;
  auto dragButton = <div style=style.textDrag data=this></div>;

  Text() {
    code=journal.ItemType.TEXT;
  }

  override void create() {
    element = <div style=style.text>
      <div style=style.nodeButtons>
        {dragButton}
        <div style=style.selectButton></div>
        <div style=style.deleteButton onclick=onDeleteClick></div>
      </div>
    </div>;
  }

  override void enter() {
    if isEnter { return; }
    base.enter();

    web.appendTag(dragHandle);
    web.dragInit(dragButton, dragHandle);
  }

  override void exit() {
    if !isEnter { return; }

    web.dragUninit(dragButton);
    dragHandle.remove();

    base.exit();
  }
}
